# receipt-recognition (WIP)

A JS app that uses opencv.js & Tesseract.js to process images of receipts and extract information from them using OCR.

1. Install dependencies:
   `npm i`

2. Run:
   `npm run start`
